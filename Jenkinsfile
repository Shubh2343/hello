pipeline {
  agent any
  stages {
    stage('Build?') {
      steps {
        echo 'First step'
        waitUntil(initialRecurrencePeriod: 1, quiet: true) {
          echo 'edsx'
        }

      }
    }

  }
}